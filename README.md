# 岁月留痕 (Fleeting Bloom Chronicles)

## 项目介绍

《岁月留痕》是一个基于 Turn.js 开发的翻页杂志项目，以"海棠花"为主题，记录高三五班学生的青春回忆与校园故事。项目采用复古文艺风格，融合米色与棕色的温暖色调，营造怀旧氛围。

## 功能特性

- **精美的翻页动画**：使用 Turn.js 实现流畅的翻页效果，模拟真实书籍的翻页体验
- **封面页面交互**：打开网页首先看到封面，点击"翻开"按钮后进入杂志内容
- **背景音乐**：集成背景音乐，增强阅读体验
- **缩略图导航**：支持横向滚动的缩略图导航，可快速定位到任意页面
- **悬停放大效果**：鼠标悬停在缩略图上时会从鼠标位置放大，提升用户体验
- **平滑滚动**：点击缩略图后会平滑滚动到选中的页面
- **响应式设计**：适配不同屏幕尺寸

## 技术实现

- **前端框架**：HTML5, CSS3, JavaScript
- **核心库**：Turn.js (翻页效果), jQuery (DOM操作)
- **音频处理**：HTML5 Audio API
- **动画效果**：CSS3 Transitions, Transformations
- **响应式设计**：Media Queries

## 目录结构

```
Haitang Memoirs/
├── css/             # 样式文件
│   ├── jquery.ui.css
│   ├── jquery.ui.html4.css
│   └── magazine.css # 主样式文件
├── extras/          # 额外资源
│   ├── Wild - All My Life.mp3 # 背景音乐
│   ├── jquery.min.1.7.js
│   ├── modernizr.2.5.3.min.js
│   └── 其他依赖库
├── js/              # 自定义脚本
│   └── magazine.js  # 杂志核心功能
├── lib/             # 核心库
│   ├── turn.js      # 翻页效果库
│   ├── zoom.min.js  # 缩放功能
│   └── 其他依赖库
├── pages/           # 页面图片
│   ├── large/       # 大图
│   ├── normal/      # 标准图
│   └── thumb/       # 缩略图
├── pics/            # 界面图片
├── index.html       # 主页面
└── README.md        # 项目说明
```

## 安装与使用

### 1. 克隆或下载项目

### 2. 启动本地服务器

由于浏览器的安全限制，直接打开 HTML 文件可能会导致某些功能无法正常工作。建议使用本地服务器运行项目：

#### 使用 Python 启动服务器

```bash
# 在项目根目录执行
python -m http.server 8000
```

#### 使用 Node.js 启动服务器

```bash
# 安装 http-server（如果未安装）
npm install -g http-server

# 在项目根目录执行
http-server -p 8000
```

### 3. 访问项目

在浏览器中打开 `http://localhost:8000/index.html` 即可访问项目。

## 使用说明

1. **打开页面**：首先看到封面，点击"翻开"按钮进入杂志内容
2. **翻页**：
   - 鼠标点击页面边缘或使用左右箭头按钮翻页
   - 触摸设备上可滑动翻页
3. **导航**：
   - 使用底部的缩略图导航栏快速定位页面
   - 鼠标滚轮可横向滚动缩略图
   - 点击缩略图直接跳转到对应页面
4. **音乐控制**：
   - 点击左上角的音乐图标控制音乐播放/暂停
5. **缩放**：
   - 点击右上角的放大/缩小图标调整页面大小

## 自定义与扩展

### 添加新页面

1. 在 `pages/large/`、`pages/normal/` 和 `pages/thumb/` 目录中添加对应的图片
2. 修改 `js/magazine.js` 中的图片加载逻辑

### 修改背景音乐

1. 将新的音乐文件替换 `extras/Wild - All My Life.mp3`
2. 确保音乐文件格式为 MP3

### 调整样式

修改 `css/magazine.css` 文件可以调整项目的视觉样式，包括颜色、字体、布局等。

## 浏览器兼容性

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 注意事项

1. **性能优化**：对于大量页面，建议使用动态加载
2. **文件路径**：确保所有文件路径正确，特别是图片和音频文件
3. **浏览器安全限制**：在本地直接打开 HTML 文件可能会导致音频无法自动播放，建议使用本地服务器
4. **响应式设计**：在不同屏幕尺寸上可能需要调整布局

## 项目英文名

**Fleeting Bloom Chronicles**

- **F** (\*) - Fleeting (短暂的、转瞬即逝的)
- **B** (\*) - Bloom (绽放)
- **C** (\*) - Chronicles (编年史)

寓意青春如海棠花般短暂而绚烂，值得被永久珍藏。
